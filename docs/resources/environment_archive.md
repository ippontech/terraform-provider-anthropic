---
page_title: "anthropic_environment_archive Resource - anthropic"
subcategory: "Environments"
description: |-
  Archives an Anthropic cloud environment (beta). Archiving is permanent. Destroying this resource is a no-op — the environment remains archived.
---

# anthropic_environment_archive (Resource)

Archives an Anthropic cloud environment (beta). Archived environments cannot be used to create new sessions.

**Important:** Archiving is a one-way operation — there is no unarchive API. Destroying this Terraform resource does **not** delete or unarchive the environment; it only removes the resource from Terraform state.

## Example Usage

```hcl
terraform {
  required_version = ">= 1.0"
  required_providers {
    anthropic = {
      source  = "registry.terraform.io/ippontech/anthropic"
      version = "~> 1.0"
    }
  }
}

resource "anthropic_environment" "example" {
  name        = "environment-to-archive"
  description = "This environment will be archived"
}

resource "anthropic_environment_archive" "example" {
  environment_id = anthropic_environment.example.id
}

output "environment_archive_id" {
  value = anthropic_environment_archive.example.id
}

output "archived_at" {
  value = anthropic_environment_archive.example.archived_at
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `environment_id` (String) ID of the environment to archive. Changing this value forces a new archive operation on the new environment.

### Read-Only

- `archived_at` (String) RFC 3339 timestamp when the environment was archived.
- `id` (String) Identifier of the archived environment (mirrors `environment_id`).

## Import

Import an existing archive record using the environment ID:

```shell
terraform import anthropic_environment_archive.example <environment_id>
```
