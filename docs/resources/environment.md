---
page_title: "anthropic_environment Resource - anthropic"
subcategory: "Environments"
description: |-
  Creates and manages a cloud environment (container template) for Anthropic Managed Agent sessions (beta).
---

# anthropic_environment (Resource)

Creates and manages a cloud environment (container template) for Anthropic Managed Agent sessions (beta). Environments define the networking policy and pre-installed packages available to agent sessions.

## Example Usage

```hcl
terraform {
  required_version = "~> 1.0"
  required_providers {
    anthropic = {
      source  = "registry.terraform.io/ippontech/anthropic"
      version = "~> 1.0"
    }
  }
}

# Minimal environment
resource "anthropic_environment" "minimal" {
  name = "minimal-environment"
}

# Environment with limited network and pip packages
resource "anthropic_environment" "python_data" {
  name        = "python-data-analysis"
  description = "Environment for Python data analysis workloads"

  metadata = {
    team = "data-science"
    env  = "production"
  }

  config = {
    networking = {
      type                   = "limited"
      allow_package_managers = true
      allowed_hosts          = ["api.example.com"]
    }
    packages = {
      pip = ["pandas", "numpy"]
    }
  }
}

# Unrestricted environment
resource "anthropic_environment" "unrestricted" {
  name = "unrestricted-environment"

  config = {
    networking = {
      type = "unrestricted"
    }
  }
}

output "environment_id" {
  value = anthropic_environment.python_data.id
}

output "environment_type" {
  value = anthropic_environment.python_data.type
}

output "environment_networking_type" {
  value = anthropic_environment.python_data.config.networking.type
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Human-readable name for the environment.

### Optional

- `config` (Attributes) Cloud environment configuration including networking and packages. (see [below for nested schema](#nestedatt--config))
- `description` (String) Optional description of the environment.
- `metadata` (Map of String) User-provided metadata key-value pairs.

### Read-Only

- `archived_at` (String) Archive timestamp (RFC 3339). Null if the environment has not been archived.
- `created_at` (String) Creation timestamp (RFC 3339).
- `id` (String) Unique environment identifier assigned by the API (e.g. `env_...`).
- `type` (String) Object type. Always `environment`.
- `updated_at` (String) Last update timestamp (RFC 3339).

<a id="nestedatt--config"></a>
### Nested Schema for `config`

Optional:

- `networking` (Attributes) Network configuration policy for the environment. (see [below for nested schema](#nestedatt--config--networking))
- `packages` (Attributes) Package manager configuration for pre-installed packages. (see [below for nested schema](#nestedatt--config--packages))

Read-Only:

- `type` (String) Environment configuration type. Always `cloud`.

<a id="nestedatt--config--networking"></a>
### Nested Schema for `config.networking`

Required:

- `type` (String) Network policy type. One of `unrestricted` or `limited`.

Optional:

- `allow_mcp_servers` (Boolean) Permits outbound access to MCP server endpoints. Only applicable for `limited` networking.
- `allow_package_managers` (Boolean) Permits outbound access to public package registries. Only applicable for `limited` networking.
- `allowed_hosts` (List of String) Specifies domains the container can reach. Only applicable for `limited` networking.


<a id="nestedatt--config--packages"></a>
### Nested Schema for `config.packages`

Optional:

- `apt` (List of String) Ubuntu/Debian packages to install.
- `cargo` (List of String) Rust packages to install.
- `gem` (List of String) Ruby packages to install.
- `go` (List of String) Go packages to install.
- `npm` (List of String) Node.js packages to install.
- `pip` (List of String) Python packages to install.

## Import

Import an existing environment using its ID:

```shell
terraform import anthropic_environment.example <environment_id>
```
