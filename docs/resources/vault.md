---
page_title: "anthropic_vault Resource - anthropic"
subcategory: "Vaults"
description: |-
  Creates and manages a vault that securely stores MCP credentials for use by Anthropic Managed Agent sessions (beta).
---

# anthropic_vault (Resource)

Creates and manages a vault that securely stores MCP credentials for use by Anthropic Managed Agent sessions (beta). Vaults are billed only at runtime when credentials are accessed during an agent session.

> **API**: `POST/GET/POST/DELETE /v1/vaults[/{id}]` (Managed Agents API).
> **Auth**: Standard API key (`api_key` / `ANTHROPIC_API_KEY`).
> **Beta header**: `anthropic-beta: managed-agents-2026-04-01`.
> **Cost**: Free for management, billable at runtime when credentials are used.

## Example Usage

```hcl
terraform {
  required_version = "~> 1.0"
  required_providers {
    anthropic = {
      source  = "registry.terraform.io/ippontech/anthropic"
      version = "~> 1.0"
    }
  }
}

# Minimal vault
resource "anthropic_vault" "minimal" {
  display_name = "my-vault"
}

# Vault with metadata
resource "anthropic_vault" "with_metadata" {
  display_name = "vault-with-metadata"

  metadata = {
    team        = "platform"
    environment = "production"
  }
}

# Vault archived instead of deleted on terraform destroy
resource "anthropic_vault" "preserved" {
  display_name       = "preserved-vault"
  archive_on_destroy = true
}

output "vault_id" {
  value = anthropic_vault.minimal.id
}

output "vault_created_at" {
  value = anthropic_vault.minimal.created_at
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `display_name` (String) Human-readable name for the vault. 1–255 characters.

### Optional

- `archive_on_destroy` (Boolean) If `true`, destroying this resource archives the vault instead of permanently deleting it. Default: `false`.
- `metadata` (Map of String) Arbitrary key-value metadata to attach to the vault. Maximum 16 pairs, keys up to 64 characters, values up to 512 characters.

### Read-Only

- `archived_at` (String) Archive timestamp (RFC 3339). Null if the vault has not been archived.
- `created_at` (String) Creation timestamp (RFC 3339).
- `id` (String) Unique vault identifier assigned by the API (e.g. `vlt_...`).
- `type` (String) Object type. Always `vault`.
- `updated_at` (String) Last update timestamp (RFC 3339).

## Import

Import an existing vault using its ID:

```shell
terraform import anthropic_vault.example <vault_id>
```
