---
page_title: "anthropic_service_account_workspaces Data Source - anthropic"
subcategory: "Service Accounts"
description: |-
  Lists the workspace memberships (implicit and explicit) of a Workload Identity Federation service account.
---

# anthropic_service_account_workspaces (Data Source)

Lists the workspace memberships (implicit and explicit) of a Workload Identity Federation service account. All pages are fetched automatically. Every service account has an implicit membership in the organization's default workspace even when it holds no explicit membership anywhere; that entry is returned as the first page's first result.

> **API**: `GET /v1/organizations/service_accounts/{service_account_id}/workspaces`.
> **Auth**: requires an org:admin OAuth bearer token (`auth_token` / `ANTHROPIC_AUTH_TOKEN`), from `ant auth login --scope org:admin` or a workload identity federation rule. Admin API keys (`admin_api_key` / `ANTHROPIC_ADMIN_API_KEY`) are **not** accepted on this endpoint.
> **Beta header**: none required.
> **Cost**: Free.

## Example Usage

```hcl
terraform {
  required_version = "~> 1.0"

  required_providers {
    anthropic = {
      source  = "registry.terraform.io/ippontech/anthropic"
      version = "~> 1.0"
    }
  }
}

# When you already know the service account ID, look it up directly:
#
#   data "anthropic_service_account_workspaces" "example" {
#     service_account_id = "svac_01WCz1FkmYMm4gnmykNKUu3Q"
#   }
#
# This example instead resolves a real service account ID from the full list,
# so it can run end-to-end, then lists that service account's workspace
# memberships.
data "anthropic_service_accounts" "all" {}

data "anthropic_service_account_workspaces" "example" {
  service_account_id = data.anthropic_service_accounts.all.service_accounts[0].id
}

output "service_account_workspaces_count" {
  description = "Total number of workspace memberships (implicit and explicit) for the service account."
  value       = length(data.anthropic_service_account_workspaces.example.workspaces)
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `service_account_id` (String) The ID of the service account whose workspace memberships to list.

### Read-Only

- `workspaces` (Attributes List) List of workspace memberships for the service account. (see [below for nested schema](#nestedatt--workspaces))

<a id="nestedatt--workspaces"></a>
### Nested Schema for `workspaces`

Read-Only:

- `created_by_actor_id` (String) Tagged ID (`user_...`/`svac_...`) of the actor who created this membership.
- `implicit` (Boolean) True when this is the implicit default-workspace membership every service account has when no explicit membership exists. Implicit memberships have role `workspace_user` and cannot be removed.
- `workspace_id` (String) Tagged workspace ID (`wrkspc_...`).
- `workspace_role` (String) Role of the service account in this workspace (e.g. `workspace_admin`, `workspace_developer`, `workspace_restricted_developer`, `workspace_user`). Service accounts cannot hold the `workspace_billing` role.
