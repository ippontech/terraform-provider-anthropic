---
page_title: "anthropic_workspace_member Data Source - anthropic"
subcategory: "Workspaces"
description: |-
  Fetches a single Anthropic workspace member by workspace ID and user ID.
---

# anthropic_workspace_member (Data Source)

Fetches a single Anthropic workspace member by workspace ID and user ID.

## Example Usage

```hcl
terraform {
  required_version = ">= 1.0"

  required_providers {
    anthropic = {
      source  = "registry.terraform.io/ippontech/anthropic"
      version = "~> 1.0"
    }
  }
}

variable "workspace_id" {}
variable "user_id" {}

data "anthropic_workspace_member" "example" {
  workspace_id = var.workspace_id
  user_id      = var.user_id
}

output "workspace_role" {
  value = data.anthropic_workspace_member.example.workspace_role
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `user_id` (String) The ID of the user.
- `workspace_id` (String) The ID of the workspace.

### Read-Only

- `type` (String) Object type. Always `workspace_member`.
- `workspace_role` (String) The role of the user in the workspace (e.g. `workspace_developer`, `workspace_admin`).
