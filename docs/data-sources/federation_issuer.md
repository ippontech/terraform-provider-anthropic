---
page_title: "anthropic_federation_issuer Data Source - anthropic"
subcategory: "Workload Identity Federation"
description: |-
  Fetches a Workload Identity Federation issuer by ID.
---

# anthropic_federation_issuer (Data Source)

Fetches a Workload Identity Federation issuer by ID, including its JWKS configuration and JWKS polling status.

> **API**: `GET /v1/organizations/federation_issuers/{id}`
> **Auth**: org:admin OAuth bearer token (`auth_token` / `ANTHROPIC_AUTH_TOKEN`). Admin API keys are not accepted.

## Example Usage

```hcl
terraform {
  required_version = ">= 1.6"

  required_providers {
    anthropic = {
      source  = "registry.terraform.io/ippontech/anthropic"
      version = "~> 1.0"
    }
  }
}

# Resolve a real federation issuer ID from the list data source: a constant ID
# would 404 against a live organization, and this also keeps the ID
# unknown-at-plan so the read genuinely happens at apply time.
data "anthropic_federation_issuers" "all" {}

data "anthropic_federation_issuer" "example" {
  id = data.anthropic_federation_issuers.all.issuers[0].id
}

output "federation_issuer_name" {
  value = data.anthropic_federation_issuer.example.name
}

output "federation_issuer_issuer_url" {
  value = data.anthropic_federation_issuer.example.issuer_url
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) The ID of the federation issuer to retrieve (`fdis_...`).

### Read-Only

- `archived_at` (String) RFC 3339 timestamp at which this issuer was archived. Null if the issuer has not been archived.
- `archived_by_actor_id` (String) Tagged ID (`user_`/`svac_`) of the actor that archived this issuer. Null if the issuer has not been archived.
- `check_jti` (Boolean) Whether the jwt-bearer exchange enforces JTI single-use (replay protection) for tokens from this issuer.
- `created_at` (String) Creation timestamp (RFC 3339).
- `created_by_actor_id` (String) Tagged ID (`user_`/`svac_`) of the actor that created this issuer.
- `issuer_url` (String) The `iss` claim value that incoming JWTs must match exactly.
- `jwks` (Attributes) How signing keys are obtained for signature verification. (see [below for nested schema](#nestedatt--jwks))
- `jwks_polling_disabled_at` (String) RFC 3339 timestamp at which Anthropic's JWKS poller paused polling for this issuer after repeated fetch failures. Null while polling is active.
- `max_jwt_lifetime_seconds` (Number) Maximum allowed iat→exp spread for assertions from this issuer, in seconds.
- `name` (String) Admin-chosen slug identifier.
- `poll_status` (Attributes) Status of automatic JWKS polling for this issuer. (see [below for nested schema](#nestedatt--poll_status))
- `updated_at` (String) Last update timestamp (RFC 3339).
- `updated_by_actor_id` (String) Tagged ID (`user_`/`svac_`) of the actor that last updated this issuer.

<a id="nestedatt--jwks"></a>
### Nested Schema for `jwks`

Read-Only:

- `ca_cert_pem` (String) Custom CA (PEM) for TLS verification of the JWKS fetch. Only populated for `discovery` and `explicit_url` modes.
- `discovery_base` (String) Set when the OIDC discovery URL differs from `issuer_url`. Only populated for `discovery` mode.
- `keys` (String) Inline JWK objects, as a JSON string. Only populated for `inline` mode.
- `type` (String) JWKS mode: `discovery`, `explicit_url`, or `inline`.
- `url` (String) Fixed JWKS endpoint. Only populated for `explicit_url` mode.


<a id="nestedatt--poll_status"></a>
### Nested Schema for `poll_status`

Read-Only:

- `consecutive_failures` (Number) Consecutive fetch failures since the last success.
- `last_fetched_at` (String) RFC 3339 timestamp of the last successful fetch. Null if never fetched.
- `next_poll_at` (String) RFC 3339 timestamp of the next scheduled fetch. Null if paused.
