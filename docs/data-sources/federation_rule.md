---
page_title: "anthropic_federation_rule Data Source - anthropic"
subcategory: "Workload Identity Federation"
description: |-
  Fetches a single Workload Identity Federation rule by ID.
---

# anthropic_federation_rule (Data Source)

Fetches a single Workload Identity Federation rule by ID. A federation rule binds an external OIDC identity (from a federation issuer) to an Anthropic service account, optionally scoped to one or more workspaces, and governs the tokens minted via token exchange.

> **API**: `GET /v1/organizations/federation_rules/{id}` (beta).
> **Auth**: OAuth bearer token with the `org:admin` scope (`auth_token` / `ANTHROPIC_AUTH_TOKEN`). Admin API keys are not accepted.
> **Beta header**: None.
> **Cost**: Free.

## Example Usage

```hcl
terraform {
  required_version = "~> 1.0"
  required_providers {
    anthropic = {
      source  = "registry.terraform.io/ippontech/anthropic"
      version = "~> 1.0"
    }
  }
}

# Resolve a real federation rule ID from the list data source, then fetch its
# full details by ID.
data "anthropic_federation_rules" "all" {}

data "anthropic_federation_rule" "example" {
  id = data.anthropic_federation_rules.all.rules[0].id
}

output "federation_rule_id" {
  value = data.anthropic_federation_rule.example.id
}

output "federation_rule_target_service_account_id" {
  value = data.anthropic_federation_rule.example.target.service_account_id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) Tagged ID of the federation rule (`fdrl_...`).

### Read-Only

- `applies_to_all_workspaces` (Boolean) When true, this rule is enabled for every workspace in the org (including ones created after the rule). `workspace_ids` is ignored at exchange time.
- `archived_at` (String) RFC 3339 timestamp of when this rule was archived. Null when not archived.
- `archived_by_actor_id` (String) Tagged ID (`user_`/`svac_`) of the actor that archived this rule. Null when not archived.
- `attributes` (Map of String) CEL expressions extracting named values from claims. Not yet supported by the API; always null.
- `created_at` (String) RFC 3339 timestamp of when this rule was created.
- `created_by_actor_id` (String) Tagged ID (`user_`/`svac_`) of the actor that created this rule.
- `description` (String) Optional free-text description.
- `issuer_id` (String) Tagged ID of the issuer whose tokens this rule accepts.
- `issuer_name` (String) Issuer's display name at read time.
- `match` (Attributes) Conditions the verified JWT must satisfy for this rule to apply. All populated matcher fields must pass. (see [below for nested schema](#nestedatt--match))
- `name` (String) Admin-chosen slug identifier.
- `oauth_scope` (String) Space-separated OAuth scopes granted on the minted token.
- `target` (Attributes) Identity that tokens minted via this rule act as. Currently always a service account target. (see [below for nested schema](#nestedatt--target))
- `token_lifetime_seconds` (Number) Lifetime in seconds of access tokens minted via this rule.
- `updated_at` (String) RFC 3339 timestamp of when this rule was last updated.
- `updated_by_actor_id` (String) Tagged ID (`user_`/`svac_`) of the actor that last updated this rule.
- `workspace_id` (String) Legacy single-workspace binding. Prefer `workspace_ids`. Null when unset.
- `workspace_ids` (List of String) Tagged IDs of the workspaces this rule is enabled for. Null when empty.

<a id="nestedatt--match"></a>
### Nested Schema for `match`

Read-Only:

- `audience` (String) Exact match against the `aud` claim. Null when unset (the issuer's default audience applies).
- `claims` (Map of String) Exact-match `{claim: value}` pairs against top-level claims. Null when unset.
- `condition` (String) CEL expression over claims for logic the structural fields can't express. Null when unset.
- `subject_prefix` (String) Match against the verified JWT `sub` claim (exact, or prefix match if it ends with `*`). Null when unset.


<a id="nestedatt--target"></a>
### Nested Schema for `target`

Read-Only:

- `service_account_id` (String) Tagged ID of the service account to mint tokens for.
- `service_account_name` (String) Service account's display name at read time. Null when unavailable.
