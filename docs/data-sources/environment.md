---
page_title: "anthropic_environment Data Source - anthropic"
subcategory: "Environments"
description: |-
  Fetches a single Anthropic cloud environment by ID (beta).
---

# anthropic_environment (Data Source)

Fetches a single Anthropic cloud environment by ID (beta).

## Example Usage

```hcl
terraform {
  required_version = ">= 1.0"
  required_providers {
    anthropic = {
      source  = "registry.terraform.io/ippontech/anthropic"
      version = "~> 0.1.0"
    }
  }
}

variable "environment_id" {
  type        = string
  description = "The ID of the environment to look up."
}

data "anthropic_environment" "example" {
  environment_id = var.environment_id
}

output "environment_id" {
  value = data.anthropic_environment.example.id
}

output "environment_name" {
  value = data.anthropic_environment.example.name
}

output "environment_type" {
  value = data.anthropic_environment.example.type
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `environment_id` (String) The ID of the environment to retrieve.

### Read-Only

- `archived_at` (String) RFC 3339 timestamp when the environment was archived. Null if not archived.
- `config` (Attributes) Cloud environment configuration. (see [below for nested schema](#nestedatt--config))
- `created_at` (String) RFC 3339 timestamp when the environment was created.
- `description` (String) User-provided description for the environment.
- `id` (String) Unique environment identifier.
- `metadata` (Map of String) Arbitrary key-value metadata attached to the environment.
- `name` (String) Human-readable name for the environment.
- `type` (String) Object type (always `environment`).
- `updated_at` (String) RFC 3339 timestamp when the environment was last updated.

<a id="nestedatt--config"></a>
### Nested Schema for `config`

Read-Only:

- `networking` (Attributes) Network configuration policy. (see [below for nested schema](#nestedatt--config--networking))
- `packages` (Attributes) Package manager configuration. (see [below for nested schema](#nestedatt--config--packages))
- `type` (String) Environment configuration type (always `cloud`).

<a id="nestedatt--config--networking"></a>
### Nested Schema for `config.networking`

Read-Only:

- `allow_mcp_servers` (Boolean) Whether outbound access to MCP server endpoints is permitted.
- `allow_package_managers` (Boolean) Whether outbound access to public package registries is permitted.
- `allowed_hosts` (List of String) Domains the container can reach.
- `type` (String) Network policy type: `unrestricted` or `limited`.


<a id="nestedatt--config--packages"></a>
### Nested Schema for `config.packages`

Read-Only:

- `apt` (List of String) Ubuntu/Debian packages to install.
- `cargo` (List of String) Rust packages to install.
- `gem` (List of String) Ruby packages to install.
- `go` (List of String) Go packages to install.
- `npm` (List of String) Node.js packages to install.
- `pip` (List of String) Python packages to install.
