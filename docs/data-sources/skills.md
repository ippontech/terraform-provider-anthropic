---
page_title: "anthropic_skills Data Source - anthropic"
subcategory: "Skills"
description: |-
  Lists available Skills on the Anthropic platform (beta). Supports optional filtering by source; all pages are fetched automatically.
---

# anthropic_skills (Data Source)

Lists available Skills on the Anthropic platform (beta). Supports optional filtering by source (`"custom"` or `"anthropic"`); all pages are fetched automatically.

## Example Usage

```hcl
terraform {
  required_version = ">= 1.0"

  required_providers {
    anthropic = {
      source  = "registry.terraform.io/ippontech/anthropic"
      version = "~> 1.0"
    }
  }
}

resource "anthropic_skill" "example" {
  display_title = "Example Skill (list)"
  files         = ["${path.module}/SKILL.md"]
}

data "anthropic_skills" "all" {
  depends_on = [anthropic_skill.example]
}

output "skills" {
  description = "List of available Anthropic skills."
  value       = data.anthropic_skills.all.skills
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `source_filter` (String) Filter skills by source. Accepted values: `"custom"` (user-created) or `"anthropic"` (built-in).

### Read-Only

- `skills` (Attributes List) List of Skills matching the filter. (see [below for nested schema](#nestedatt--skills))

<a id="nestedatt--skills"></a>
### Nested Schema for `skills`

Read-Only:

- `created_at` (String) ISO 8601 timestamp of when the skill was created.
- `display_title` (String) Human-readable display title for the skill.
- `id` (String) Unique skill identifier assigned by the API.
- `latest_version` (String) The latest version identifier for the skill.
- `source` (String) Source of the skill: `"custom"` for user-created skills, `"anthropic"` for built-in skills.
- `type` (String) Object type returned by the API.
- `updated_at` (String) ISO 8601 timestamp of when the skill was last updated.
