---
page_title: "anthropic_environments Data Source - anthropic"
subcategory: "Environments"
description: |-
  Lists all Anthropic cloud environments (beta). All pages are fetched automatically.
---

# anthropic_environments (Data Source)

Lists all Anthropic cloud environments (beta). All pages are fetched automatically.

## Example Usage

```hcl
terraform {
  required_version = ">= 1.0"
  required_providers {
    anthropic = {
      source  = "registry.terraform.io/ippontech/anthropic"
      version = "~> 1.0"
    }
  }
}

data "anthropic_environments" "all" {}

output "environments_count" {
  value = length(data.anthropic_environments.all.environments)
}

output "environment_names" {
  value = [for e in data.anthropic_environments.all.environments : e.name]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `include_archived` (Boolean) When true, archived environments are included in the results.

### Read-Only

- `environments` (Attributes List) List of environments. (see [below for nested schema](#nestedatt--environments))

<a id="nestedatt--environments"></a>
### Nested Schema for `environments`

Read-Only:

- `archived_at` (String) Archive timestamp (RFC 3339). Null if the environment has not been archived.
- `config` (Attributes) Cloud environment configuration. (see [below for nested schema](#nestedatt--environments--config))
- `created_at` (String) Creation timestamp (RFC 3339).
- `description` (String) Optional description of the environment.
- `id` (String) Unique environment identifier (e.g. `env_...`).
- `metadata` (Map of String) User-provided metadata key-value pairs.
- `name` (String) Human-readable name for the environment.
- `type` (String) Object type. Always `environment`.
- `updated_at` (String) Last update timestamp (RFC 3339).

<a id="nestedatt--environments--config"></a>
### Nested Schema for `environments.config`

Read-Only:

- `networking` (Attributes) Network configuration policy. (see [below for nested schema](#nestedatt--environments--config--networking))
- `packages` (Attributes) Package manager configuration. (see [below for nested schema](#nestedatt--environments--config--packages))
- `type` (String) Environment configuration type. Always `cloud`.

<a id="nestedatt--environments--config--networking"></a>
### Nested Schema for `environments.config.networking`

Read-Only:

- `allow_mcp_servers` (Boolean) Permits outbound access to MCP server endpoints.
- `allow_package_managers` (Boolean) Permits outbound access to public package registries.
- `allowed_hosts` (List of String) Specifies domains the container can reach.
- `type` (String) Network policy type. One of `unrestricted` or `limited`.


<a id="nestedatt--environments--config--packages"></a>
### Nested Schema for `environments.config.packages`

Read-Only:

- `apt` (List of String) Ubuntu/Debian packages to install.
- `cargo` (List of String) Rust packages to install.
- `gem` (List of String) Ruby packages to install.
- `go` (List of String) Go packages to install.
- `npm` (List of String) Node.js packages to install.
- `pip` (List of String) Python packages to install.
