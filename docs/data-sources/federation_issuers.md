---
page_title: "anthropic_federation_issuers Data Source - anthropic"
subcategory: "Workload Identity Federation"
description: |-
  Lists OIDC issuers registered for Workload Identity Federation in the organization (beta), with transparent pagination.
---

# anthropic_federation_issuers (Data Source)

Lists OIDC issuers registered for Workload Identity Federation in the organization (beta). All pages are fetched automatically. Archived issuers are excluded unless `include_archived` is set to `true`.

> **API**: `GET /v1/organizations/federation_issuers` (beta).
> **Auth**: org:admin OAuth bearer token (`auth_token` / `ANTHROPIC_AUTH_TOKEN`). Admin API keys are not accepted.
> **Beta header**: None.
> **Cost**: Free.

## Example Usage

```hcl
terraform {
  required_version = "~> 1.0"
  required_providers {
    anthropic = {
      source  = "registry.terraform.io/ippontech/anthropic"
      version = "~> 1.0"
    }
  }
}

# Requires an org:admin OAuth bearer token (auth_token / ANTHROPIC_AUTH_TOKEN);
# Admin API keys are not accepted by this endpoint.
data "anthropic_federation_issuers" "all" {}

output "federation_issuer_ids" {
  value = [for issuer in data.anthropic_federation_issuers.all.issuers : issuer.id]
}

output "federation_issuers_count" {
  value = length(data.anthropic_federation_issuers.all.issuers)
}

# Include archived issuers too.
data "anthropic_federation_issuers" "with_archived" {
  include_archived = true
}

output "federation_issuers_count_including_archived" {
  value = length(data.anthropic_federation_issuers.with_archived.issuers)
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `include_archived` (Boolean) Whether to include archived issuers in the results. Defaults to `false`.

### Read-Only

- `issuers` (Attributes List) List of federation issuers matching the filter. (see [below for nested schema](#nestedatt--issuers))

<a id="nestedatt--issuers"></a>
### Nested Schema for `issuers`

Read-Only:

- `archived_at` (String) When this issuer was archived (RFC 3339). Null if the issuer has not been archived.
- `archived_by_actor_id` (String) Tagged ID (`user_`/`svac_`) of the actor that archived this issuer. Null if the issuer has not been archived.
- `check_jti` (Boolean) Whether the jwt-bearer exchange enforces JTI single-use (replay protection) for tokens from this issuer.
- `created_at` (String) When this issuer was created (RFC 3339).
- `created_by_actor_id` (String) Tagged ID (`user_`/`svac_`) of the actor that created this issuer.
- `id` (String) Tagged ID of the federation issuer.
- `issuer_url` (String) The `iss` claim value. Incoming JWTs must match exactly.
- `jwks` (Attributes) How signing keys are obtained for signature verification. (see [below for nested schema](#nestedatt--issuers--jwks))
- `jwks_polling_disabled_at` (String) If set, Anthropic's JWKS poller has paused polling for this issuer after repeated fetch failures (RFC 3339). Null while polling is active.
- `max_jwt_lifetime_seconds` (Number) Maximum allowed iat→exp spread for assertions from this issuer, in seconds.
- `name` (String) Admin-chosen slug identifier.
- `poll_status` (Attributes) Status of automatic JWKS polling for this issuer. (see [below for nested schema](#nestedatt--issuers--poll_status))
- `updated_at` (String) When this issuer was last updated (RFC 3339).
- `updated_by_actor_id` (String) Tagged ID (`user_`/`svac_`) of the actor that last updated this issuer.

<a id="nestedatt--issuers--jwks"></a>
### Nested Schema for `issuers.jwks`

Read-Only:

- `ca_cert_pem` (String) Optional custom CA (PEM) for TLS verification of the JWKS fetch. Only set for `discovery` and `explicit_url` modes.
- `discovery_base` (String) Set when the discovery URL differs from `issuer_url`. Only set for `discovery` mode.
- `keys` (String) Inline JWK objects, encoded as a JSON array string. Only set for `inline` mode.
- `type` (String) JWKS mode: `discovery`, `explicit_url`, or `inline`.
- `url` (String) JWKS endpoint. Only set for `explicit_url` mode.


<a id="nestedatt--issuers--poll_status"></a>
### Nested Schema for `issuers.poll_status`

Read-Only:

- `consecutive_failures` (Number) Consecutive fetch failures since the last success.
- `last_fetched_at` (String) When the last successful fetch completed (RFC 3339). Null if never fetched.
- `next_poll_at` (String) When the next fetch is scheduled (RFC 3339). Null if paused.
